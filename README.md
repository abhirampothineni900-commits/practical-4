iterative function

summary:here the function uses for loop and while loop for this function to solve the problem repeatedly it uses and solve the problem and the speed is fast when compare to the recursive function

conclusion:space complexity it takes 0(1) and loop performs the repeatation

recursive function

summary:A function solves a problem by calling itself with a smaller problem and speed Can be slightly slower due to function calls

conclusion:Here, factorial() calls itself. the recursive version uses O(n) stack space.
